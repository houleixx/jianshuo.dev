# reco is served by its own Worker (voicedrop-reco on /reco/*), not Pages.
# Keep its source + 200MB node_modules out of the Pages asset upload.
reco/
node_modules
# claude-agent is a standalone Node service on the Tokyo VPS, not Pages.
claude-agent/
# Claude Code worktree storage — git worktrees (each with its own node_modules) live here.
.claude/
paint/
codex-agent/
