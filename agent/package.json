{
  "name": "voicedrop-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "dependencies": {
    "agents": "latest"
  },
  "devDependencies": {
    "wrangler": "^4",
    "vitest": "^2"
  }
}
